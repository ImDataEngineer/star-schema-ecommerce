# Environment
.env
.env.*
!.env.example

# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.pytest_cache/
.ruff_cache/
.mypy_cache/
.venv/
venv/

# dbt artefacts (regenerated by `dbt build`)
target/
dbt_packages/
logs/

# Local dbt profile (the learner copies profiles.yml.example here; secrets stay out of git)
.dbt/profiles.yml

# Generated fixtures (regenerated by the seeded script — large CSVs are
# *not* committed; the post-create script rebuilds them deterministically).
fixtures/oltp_dump/*.csv

# OS / IDE
.DS_Store
Thumbs.db
.vscode/
.idea/
